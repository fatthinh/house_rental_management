server:
  port: 8030
spring:
  data:
    mongodb:
      username: lpthinh
      password: lpthinh
      host: localhost
      port: 27017
      database: socialdb
      authentication-database: admin
  kafka:
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: messageResponse:com.lpthinh.socialservice.message.MessageResponse


application:
  config:
    identity-url: http://localhost:8222/api/v1/identity
